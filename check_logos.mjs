import fs from "fs/promises"; async function checkLogos() { try { const files = ["public/logo.png", "public/icons/logoMichael.png", "public/logo icon.png"]; for (const file of files) { try { await fs.access(file); console.log(`File ${file}: Present`); } catch { console.log(`File ${file}: MISSING (May cause rendering issues in MapClient.tsx)`); } } } catch (e) { console.error("Unexpected error:", e.message); } } checkLogos();
