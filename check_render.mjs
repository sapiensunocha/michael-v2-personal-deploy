import dotenv from "dotenv"; dotenv.config(); console.log("Simulating Map/index.tsx rendering..."); const mapboxToken = process.env.NEXT_PUBLIC_MAPBOX_TOKEN; const hasData = true; const isMapReady = mapboxToken ? true : false; console.log("Rendering conditions: Mapbox Token:", mapboxToken ? "Present" : "MISSING", ", Data available:", hasData, ", isMapReady:", isMapReady); console.log("Next step: Run npm run dev, visit http://localhost:3000/map, open browser console (F12), and check for Leaflet/Redux errors.");
